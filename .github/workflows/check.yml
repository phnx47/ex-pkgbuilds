name: ☣ Check Versions
on:
  schedule:
    - cron: "55 */8 * * *"
  push:
    branches:
      - "ci/check"

jobs:
  check:
    name: ☣ Check Versions
    runs-on: ubuntu-20.04
    steps:
      - name: 🚚 Checkout
        uses: actions/checkout@master
        with:
          fetch-depth: 0

      - name: 🛠 Install dependencies
        run: sudo apt-get install libssl-dev libcurl4-openssl-dev

      - name: 🔧 Install nvchecker
        run: pip install nvchecker

      - name: ☢ Check versions
        run: echo "NEW_VER=$(./check.sh)" >> $GITHUB_ENV

      - name: ✉ Test
        run: echo ${{ env.NEW_VER }}
